03/01/2018 / 03/02/2018
	-removed get_next_line.c file, asm.h reference/struct, Makefile reference
	-added stdbool.h to asm.h

	-removed getfilecontents(), getfilesize(), gfc_quit functions - unused
	-removed is_digit(), changing to ft_isdigit() which is in riking/libft
	-changed is_header_complete() to return booleans
	-changed ft_isspace() to return booleans
	-changed is_instruction() to return booleans
	-changed is_direct(), is_indirect(), is_register() to return booleans
	-renamed is_operand() to is_valid_operand(), now returns booleans
	-removed does_label_exist(), will check if find_label() returns ptr/NULL for validation
	-changed is_label(), is_direct_label(), is_indirect_label() to return booleans
	-changed string_is_integer(), string_is_label() to return boolean types
	-renamed check_magic() to is_valid_magic(), now returns boolean
	-added MAX_INSTR_BYTES definition to asm.h, changed hexdump.c to allocate smaller buffer
	-added BINARY_EXTENSION definition to asm.h, some changes to main.c output file name routine
	-changed function name from instruction_size() to add_operand_size()
	-added few color codes to asm.h
	-made operand errors more verbose and fatal
	-updated root Makefile
	-norme
	--probably more/other that i forgot to document


